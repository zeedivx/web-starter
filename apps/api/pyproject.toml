[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.128.4",
]

[dependency-groups]
dev = [
    "alembic>=1.18.3",
    "ruff>=0.15.0",
    "ty>=0.0.15",
]
